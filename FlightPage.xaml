<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Uhrzeitrechner.FlightPage"
             Title="Flugzeit">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <VerticalStackLayout.GestureRecognizers>
                <SwipeGestureRecognizer Direction="Left" Swiped="OnSwipeLeft" />
                <SwipeGestureRecognizer Direction="Right" Swiped="OnSwipeRight" />
            </VerticalStackLayout.GestureRecognizers>

            <!-- Uhr -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <VerticalStackLayout Grid.Column="0">
                    <Label Text="UTC" FontSize="20" TextColor="Gray" />
                    <Label x:Name="UtcTimeLabel" Text="--:--:--" FontSize="28" FontAttributes="Bold" />
                </VerticalStackLayout>
                <VerticalStackLayout Grid.Column="1">
                    <Label Text="Lokal" FontSize="20" TextColor="Gray" />
                    <Label x:Name="LocalTimeLabel" Text="--:--:--" FontSize="28" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Grid>

            <BoxView HeightRequest="1" Color="LightGray" />

            <!-- Flugdaten -->
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" RowDefinitions="Auto,Auto" RowSpacing="8">
                <Label Grid.Row="0" Grid.Column="0" FontSize="20" Text="Datum:" VerticalOptions="Center" TextColor="Gray" />
                <Label Grid.Row="0" Grid.Column="1" FontSize="20" x:Name="DateLabel" Text="—" VerticalOptions="Center" FontAttributes="Bold" />
                <Label Grid.Row="1" Grid.Column="0" FontSize="20" Text="Flug:" VerticalOptions="Center" TextColor="Gray" />
                <Entry Grid.Row="1" Grid.Column="1" FontSize="20" x:Name="RegistrationEntry" Placeholder="z.B. D-MSSE" TextChanged="OnRegistrationChanged" />
            </Grid>

            <!-- Erfassungs-Buttons -->
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="10" RowSpacing="10">
                <Button Grid.Row="0" Grid.Column="0" x:Name="OffBlockButton" Text="Off-Block" Clicked="OnOffBlockClicked" FontAttributes="Bold" FontSize="20" HeightRequest="55" />
                <Button Grid.Row="0" Grid.Column="1" x:Name="OnBlockButton" Text="On-Block" Clicked="OnOnBlockClicked" FontAttributes="Bold" FontSize="20" HeightRequest="55" />
                <Button Grid.Row="1" Grid.Column="0" x:Name="StartButton" Text="Start" Clicked="OnStartClicked" FontAttributes="Bold" FontSize="20" HeightRequest="55" />
                <Button Grid.Row="1" Grid.Column="1" x:Name="LandingButton" Text="Landung" Clicked="OnLandingClicked" FontAttributes="Bold" FontSize="20" HeightRequest="55" />
            </Grid>

            <Button x:Name="UndoButton" Text="Letzte Erfassung rückgängig" Clicked="OnUndoClicked"
                    BackgroundColor="Transparent" FontSize="18" TextColor="Orange"
                    BorderColor="Orange" BorderWidth="1" />

            <BoxView HeightRequest="1" Color="LightGray" />

            <!-- Summen -->
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="6">
                <Label Grid.Row="0" Grid.Column="0" Text="Blockzeit:" FontSize="18" FontAttributes="Bold" />
                <Label Grid.Row="0" Grid.Column="1" x:Name="BlockTimeLabel" Text="—" FontSize="22" FontAttributes="Bold" TextColor="DodgerBlue" />
                <Label Grid.Row="1" Grid.Column="0" Text="Flugzeit:" FontSize="18" FontAttributes="Bold" />
                <Label Grid.Row="1" Grid.Column="1" x:Name="FlightTimeLabel" Text="0:00" FontSize="22" FontAttributes="Bold" TextColor="DodgerBlue" />
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0" x:Name="ResetButton" Text="Zurücksetzen" FontSize="20" Clicked="OnResetClicked"
                        BackgroundColor="Transparent" TextColor="Red"
                        BorderColor="Red" BorderWidth="1" />
                <Button Grid.Column="1" x:Name="SaveButton" Text="Flug speichern" FontSize="20" Clicked="OnSaveClicked" />
            </Grid>

            <BoxView HeightRequest="1" Color="LightGray" />

            <!-- Legs -->
            <CollectionView x:Name="LegsView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,6">
                            <Label Text="{Binding Display}" FontSize="19" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
