<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Uhrzeitrechner.MainPage"
             Title="Stundenrechner">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="20" RowSpacing="15">

        <Grid.GestureRecognizers>
            <SwipeGestureRecognizer Direction="Left" Swiped="OnSwipeLeft" />
        </Grid.GestureRecognizers>


        <!-- Eingabe -->
        <VerticalStackLayout Grid.Row="0" Spacing="20">

            <!-- Aktuelle Zeiten -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <HorizontalStackLayout Grid.Column="0" Spacing="8" VerticalOptions="Center">
                    <Label Text="UTC" FontSize="18" TextColor="Gray" VerticalOptions="Center" />
                    <Label x:Name="UtcTimeLabel" Text="--:--:--" FontSize="26" FontAttributes="Bold" VerticalOptions="Center" />
                </HorizontalStackLayout>
                <HorizontalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                    <Label Text="Lokal" FontSize="18" TextColor="Gray" VerticalOptions="Center" />
                    <Label x:Name="LocalTimeLabel" Text="--:--:--" FontSize="26" FontAttributes="Bold" VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Grid>

            <Label Text="Zeit hinzufügen" FontSize="20" FontAttributes="Bold" />
            <Grid ColumnDefinitions="*,Auto,*,Auto" ColumnSpacing="8">
                <Entry x:Name="HoursEntry"
               Grid.Column="0"
               Placeholder="Std"
               FontSize="20"
               Keyboard="Numeric"
               MaxLength="5" />
                <Label Grid.Column="1" Text=":" VerticalOptions="Center" FontSize="20" />
                <Entry x:Name="MinutesEntry"
               Grid.Column="2"
               Placeholder="Min"
               FontSize="20"
               Keyboard="Numeric"
               MaxLength="3" />
                <Button Grid.Column="3"
                Text="+"
                FontSize="20"
                FontAttributes="Bold"
                WidthRequest="55"
                HeightRequest="55"
                Clicked="OnAddClicked" />
            </Grid>
        </VerticalStackLayout>

        <!-- Trenner -->
        <BoxView Grid.Row="1" HeightRequest="1" Color="LightGray" />

        <!-- Liste der Einträge -->
        <CollectionView Grid.Row="2" x:Name="EntriesView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,Auto" Padding="5,2">
                        <Label Grid.Column="0"
                               Text="{Binding Display}"
                               FontSize="20"
                               VerticalOptions="Center" />
                        <Button Grid.Column="1"
                                Text="✕"
                                FontSize="20"
                                WidthRequest="40"
                                HeightRequest="40"
                                Padding="0"
                                BackgroundColor="Transparent"
                                TextColor="Red"
                                Clicked="OnRemoveClicked"
                                CommandParameter="{Binding .}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Summe -->
        <VerticalStackLayout Grid.Row="3" Spacing="10">
            <BoxView HeightRequest="1" Color="LightGray" />
            <Grid ColumnDefinitions="*,Auto">
                <Label Grid.Column="0" Text="Summe:" FontSize="22" FontAttributes="Bold" />
                <Label Grid.Column="1" x:Name="TotalLabel" Text="0:00"
                       FontSize="22" FontAttributes="Bold" TextColor="DodgerBlue" />
            </Grid>
            <Button Text="Alles löschen" FontSize="20" Clicked="OnClearClicked" />
        </VerticalStackLayout>

    </Grid>
</ContentPage>
